class PackageInfo {
  final String name;
  final String version;
  final String description;
  final String url;

  const PackageInfo({
    required this.name,
    required this.version,
    required this.description,
    required this.url,
  });
}
